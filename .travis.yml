dist: xenial

addons:
  apt:
    update: true
    sources:
      - ppa:longsleep/golang-backports
    packages:
      - build-essential
      - golang-go

before_script:
  - source vendor-env.sh
  # for nasm
  - export PATH=$PATH:$GOFF_PREFIX/bin
  - ./vendor-all.sh

script:
  - go test -v ./...

